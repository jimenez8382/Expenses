@model IEnumerable<Expenses.Data.Models.Expenses>
@{
    ViewBag.Title = "Home Page";
}
<div class="row" style="padding-top:0px">
    <div id="PartialViewListContainer">
        @Html.Partial("PartialViewList", Model)
    </div>
    <div class="col-md-4 right">
        <br />
        <a href="@Url.Action("Create", "Expenses" )" class="waves-effect light-blue  btn">Add New</a>
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        $('.datepicker').pickadate({
            selectMonths: true, // Creates a dropdown to control month
            selectYears: 15 // Creates a dropdown of 15 years to control year
        });
        $(".datepicker").val(new Date());
    });
</script>

