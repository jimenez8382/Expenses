@model Expenses.Data.Models.Expenses
@{
}
<form id="form-myExpenses" action="@Url.Action("Create")" method="POST" enctype="multipart/form-data" class="col s12">
    <div class="row">
        <nav class="light-blue lighten-1" role="navigation">
            <div class="nav-wrapper container">New
                <h4></h4>
            </div>
        </nav>
        @Html.Partial("PartialForm", Model)
    </div>
</form>